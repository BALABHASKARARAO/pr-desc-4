name: close-pr
on:
  pull_request:
    types:
      - opened
   
jobs:
  check-description:
    runs-on: ubuntu-latest
    steps:
	  - name: pr-desc-test
	      uses: actions/checkout@v1
          uses: nouzbe/check-pull-request-description@v1.11
		  
	  - name: close-pr-description-failed
	    id :vars
	    run: echo "bala, job sucess"
